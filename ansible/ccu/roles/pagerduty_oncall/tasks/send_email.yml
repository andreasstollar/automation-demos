- name: Send email via SMTP
  community.general.mail:
    host: "{{ smtp_host }}"
    port: "{{ smtp_port }}"
    to: "{{ mail_to }}"
    from: "{{ mail_from }}"
    subject: "🔔 Engineering On-Call Schedule"
    body: >-
      <html><body>
      <h2>IT On-Call Escalation – Level 1</h2>
      {{ oncall_summary }}
      </body></html>
    subtype: html
  delegate_to: localhost
